---
// Modal del carrito de compras
---

<div 
  id="cart-modal" 
  class="fixed inset-0 bg-black bg-opacity-50 z-50 items-center justify-center p-4 hidden opacity-0 transition-all duration-300"
  style="display: none;"
>
  <div 
    id="cart-modal-content"
    class="bg-gray-900 rounded-xl shadow-2xl max-w-md w-full max-h-[90vh] overflow-hidden transform scale-95 transition-transform duration-300"
  >
    <!-- Header del Modal -->
    <div class="flex items-center justify-between p-6 border-b border-gray-700">
      <div class="flex items-center space-x-3">
        <svg class="w-6 h-6 text-blue-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 3h2l.4 2M7 13h10l4-8H5.4m0 0L7 13m0 0l-1.5 6M7 13l-1.5 6m0 0h9M17 13v6a2 2 0 01-2 2H9a2 2 0 01-2-2v-6"></path>
        </svg>
        <h2 class="text-xl font-bold text-white">Mi Pedido</h2>
        <span id="cart-badge" class="bg-blue-500 text-white text-xs px-2 py-1 rounded-full font-medium">0</span>
      </div>
      <button 
        id="close-cart-modal"
        class="p-2 hover:bg-gray-800 rounded-lg transition-colors"
      >
        <svg class="w-6 h-6 text-gray-400 hover:text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
        </svg>
      </button>
    </div>

    <!-- Contenido del Carrito -->
    <div class="flex-1 overflow-y-auto max-h-96">
      <!-- Estado Vacío -->
      <div id="cart-empty" class="p-8 text-center hidden">
        <svg class="w-16 h-16 text-gray-600 mx-auto mb-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 3h2l.4 2M7 13h10l4-8H5.4m0 0L7 13m0 0l-1.5 6M7 13l-1.5 6m0 0h9M17 13v6a2 2 0 01-2 2H9a2 2 0 01-2-2v-6"></path>
        </svg>
        <h3 class="text-lg font-medium text-gray-300 mb-2">Tu carrito está vacío</h3>
        <p class="text-sm text-gray-500">Añade algunos platos deliciosos a tu pedido</p>
      </div>

      <!-- Lista de Items -->
      <div id="cart-items" class="space-y-3 p-4"></div>
    </div>

    <!-- Footer con Totales y Acciones -->
    <div id="cart-footer" class="border-t border-gray-700 p-6 space-y-4 hidden">
      <!-- Resumen -->
      <div class="space-y-2">
        <div class="flex justify-between text-sm">
          <span class="text-gray-400">Subtotal (<span id="total-items">0</span> items)</span>
          <span class="text-white font-medium">$<span id="subtotal">0.00</span></span>
        </div>
      </div>

      <!-- Botones de Acción -->
      <div class="space-y-3">
        <button 
          id="place-order-btn"
          class="w-full bg-green-600 hover:bg-green-700 text-white font-medium py-3 px-4 rounded-lg transition-colors flex items-center justify-center space-x-2"
        >
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
          </svg>
          <span>Realizar Pedido</span>
        </button>
        
        <button 
          id="clear-cart-btn"
          class="w-full bg-gray-700 hover:bg-gray-600 text-gray-300 font-medium py-2 px-4 rounded-lg transition-colors text-sm"
        >
          Limpiar Carrito
        </button>
      </div>
    </div>
  </div>
</div>

<!-- Loading Modal -->
<div 
  id="order-loading-modal" 
  class="fixed inset-0 bg-black bg-opacity-70 z-50 items-center justify-center p-4"
  style="display: none;"
>
  <div class="bg-gray-900 rounded-xl shadow-2xl p-8 text-center max-w-sm w-full">
    <div class="animate-spin w-12 h-12 border-4 border-blue-500 border-t-transparent rounded-full mx-auto mb-4"></div>
    <h3 class="text-lg font-medium text-white mb-2">Procesando Pedido</h3>
    <p class="text-sm text-gray-400">Por favor espera mientras creamos tu pedido...</p>
  </div>
</div>

<style>
  /* Animaciones suaves para el modal */
  .modal-enter {
    opacity: 1;
  }
  
  .modal-enter .cart-modal-content {
    transform: scale(1);
  }

  /* Scrollbar personalizado para el contenido del carrito */
  #cart-items::-webkit-scrollbar {
    width: 4px;
  }

  #cart-items::-webkit-scrollbar-track {
    background: #374151;
    border-radius: 2px;
  }

  #cart-items::-webkit-scrollbar-thumb {
    background: #6B7280;
    border-radius: 2px;
  }

  #cart-items::-webkit-scrollbar-thumb:hover {
    background: #9CA3AF;
  }
</style>
